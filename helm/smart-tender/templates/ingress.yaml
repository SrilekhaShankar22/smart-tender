apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: smart-tender-ingress
  namespace: {{ .Values.global.namespace }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: smarttender.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: angular-frontend
                port:
                  number: 80
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: auth-service
                port:
                  number: {{ .Values.services.auth.port }}
          - path: /search
            pathType: Prefix
            backend:
              service:
                name: tender-search-service
                port:
                  number: {{ .Values.services.search.port }}
